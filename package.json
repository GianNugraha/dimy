{
  "name": "test-dimy",
  "version": "1.0.0",
  "description": "test simple crud",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "up": "sequelize-cli db:migrate && sequelize-cli db:seed:all",
    "down": "sequelize-cli db:migrate:undo:all",
    "upp": "npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all",
    "downp": "npx sequelize-cli db:migrate:undo:all",
    "heroku": "git push heroku development:master"
  },
  "author": "Gian Nugraha",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.8.0",
    "postgres": "^3.3.1",
    "sequelize": "^6.25.5"
  }
}
